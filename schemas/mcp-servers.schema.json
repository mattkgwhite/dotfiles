{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/mcp-servers.schema.json",
  "title": "Canonical MCP server definitions",
  "type": "object",
  "additionalProperties": false,
  "required": ["mcp"],
  "properties": {
    "mcp": {
      "type": "object",
      "additionalProperties": false,
      "required": ["serversById"],
      "properties": {
        "serversById": {
          "type": "object",
          "additionalProperties": false,
          "patternProperties": {
            "^[a-z0-9][a-z0-9-]*$": {
              "$ref": "#/$defs/mcpServer"
            }
          }
        }
      }
    }
  },
  "$defs": {
    "stringMap": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },
    "envMap": {
      "type": "object",
      "additionalProperties": {
        "type": ["string", "number", "boolean"]
      }
    },
    "localConfig": {
      "type": "object",
      "additionalProperties": false,
      "required": ["command", "args"],
      "properties": {
        "command": { "type": "string", "minLength": 1 },
        "args": {
          "type": "array",
          "items": { "type": "string" }
        },
        "env": { "$ref": "#/$defs/envMap" }
      }
    },
    "remoteConfig": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url"],
      "properties": {
        "url": { "type": "string", "format": "uri" },
        "transport": { "type": "string" },
        "headers": { "$ref": "#/$defs/stringMap" }
      }
    },
    "conditions": {
      "type": "object",
      "additionalProperties": true
    },
    "targetConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" }
      }
    },
    "mcpServer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["enabled"],
      "properties": {
        "enabled": { "type": "boolean" },
        "conditions": { "$ref": "#/$defs/conditions" },
        "targets": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "opencode": { "$ref": "#/$defs/targetConfig" },
            "cursor": { "$ref": "#/$defs/targetConfig" }
          }
        },
        "local": { "$ref": "#/$defs/localConfig" },
        "remote": { "$ref": "#/$defs/remoteConfig" }
      },
      "oneOf": [
        { "required": ["local"] },
        { "required": ["remote"] }
      ]
    }
  }
}
